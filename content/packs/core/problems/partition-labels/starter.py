def partition_sizes(text):
    """Return the sizes of the largest number of parts with no shared letters.

    Args:
        text: a lowercase string.

    Returns:
        The size of each part, in order.
    """
    pass
