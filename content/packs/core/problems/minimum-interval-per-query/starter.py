def shortest_covering(intervals, queries):
    """Return the size of the shortest interval covering each query.

    Intervals are inclusive at both ends, so [2, 4] has size 3.

    Args:
        intervals: [start, end] pairs in arbitrary order.
        queries: the values to look up.

    Returns:
        One size per query, in the queries' original order, with -1 where nothing covers it.
    """
    pass
