def round_trip(words):
    """Encode a list of strings as one string, then decode it back.

    Args:
        words: list of strings. Any characters, including digits and '#'.

    Returns:
        A two-element list: the encoded string, and the decoded list of words.
    """
    pass
