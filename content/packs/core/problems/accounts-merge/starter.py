def merge_accounts(accounts):
    """Merge accounts that share any email address.

    Args:
        accounts: list of [name, email, ...] lists, each with at least one email.

    Returns:
        One entry per person, each being their name followed by their emails in
        sorted order. The list of entries is itself sorted.
    """
    pass
