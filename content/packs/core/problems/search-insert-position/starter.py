def insert_position(values, target):
    """Return target's index, or where it would be inserted.

    Args:
        values: a strictly ascending list.
        target: the value to locate or place.

    Returns:
        An index in [0, len(values)].
    """
    pass
