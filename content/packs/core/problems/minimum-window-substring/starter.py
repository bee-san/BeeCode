def minimum_window(haystack, needle):
    """Return the shortest substring of haystack containing all of needle.

    Args:
        haystack: the string to search.
        needle: the required characters, with multiplicity.

    Returns:
        The shortest qualifying substring, earliest one on a tie, or "" if none.
    """
    pass
