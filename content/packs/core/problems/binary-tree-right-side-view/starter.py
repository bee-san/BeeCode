def right_side_view(tree):
    """Return the rightmost value on each level, top level first.

    Args:
        tree: the tree as a level-order list, with None for absent nodes.

    Returns:
        A list with one value per level of the tree.
    """
    pass
