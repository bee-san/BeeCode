def matrix_contains(grid, target):
    """Return True if target appears in the row-wise sorted matrix.

    Args:
        grid: list of equal-length rows. Each row is sorted ascending, and every
            row starts above where the previous row ended.
        target: the value to find.

    Returns:
        True if target is present, otherwise False.
    """
    pass
