def round_trip(tree):
    """Serialize a binary tree to a string and parse it back.

    Args:
        tree: the tree as a level-order list, with None for absent nodes.

    Returns:
        A two-element list [encoded, decoded]. encoded is the comma-joined
        pre-order walk with "#" for absent nodes; decoded is the tree parsed back
        out of encoded, as a level-order list with trailing Nones omitted.
    """
    pass
