def can_open_all(rooms):
    """Return whether every room is reachable from room 0.

    Args:
        rooms: rooms[i] lists the room numbers whose keys are in room i.

    Returns:
        True if all rooms can be entered.
    """
    pass
