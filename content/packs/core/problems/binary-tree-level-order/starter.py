def level_order(tree):
    """Return the values of a level-order encoded binary tree, grouped by depth.

    Args:
        tree: level-order list of node values, with None for absent nodes.

    Returns:
        A list of lists: one per level, top to bottom, left to right.
    """
    pass
