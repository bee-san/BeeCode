def reverse_groups(values, k):
    """Return values with each full group of k elements reversed.

    Args:
        values: list of integers, possibly empty.
        k: the group size, at least 1.

    Returns:
        The list with every complete group of k reversed and any short trailing
        group left in its original order.
    """
    pass
