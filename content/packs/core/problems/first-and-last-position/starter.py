def value_span(values, target):
    """Return the first and last index of target, or [-1, -1] if absent.

    Args:
        values: a list sorted non-decreasing.
        target: the value to locate.

    Returns:
        A two-element list [first, last].
    """
    pass
