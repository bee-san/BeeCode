def summarise(values):
    """Return the sorted list of range strings covering exactly the given values.

    Args:
        values: a strictly ascending list of integers, possibly empty.

    Returns:
        A list of strings, each "start->end" or "start".
    """
    pass
