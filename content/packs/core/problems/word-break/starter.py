def can_split(text, dictionary):
    """Return True if text can be split into a sequence of dictionary words.

    Words may be reused, and every character must be used in order.

    Args:
        text: the string to split.
        dictionary: the allowed words.

    Returns:
        True if a valid split exists, else False.
    """
    pass
