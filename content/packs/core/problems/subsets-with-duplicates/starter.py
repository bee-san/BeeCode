def subsets_with_duplicates(nums):
    """Return every distinct subset of a list that may contain repeated values.

    Args:
        nums: integers, possibly with repeats, in any order.

    Returns:
        A list of subsets, each sorted ascending, with no two being reorderings of
        each other. The outer order is not judged.
    """
    pass
