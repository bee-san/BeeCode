def count_sign_assignments(values, target):
    """Return how many + / - assignments over values sum to target.

    Args:
        values: non-negative integers. Position distinguishes equal values.
        target: the sum to reach.

    Returns:
        The number of sign assignments producing target.
    """
    pass
