def reverse_values(values):
    """Return the values in reverse order.

    Args:
        values: list of integers, possibly empty.

    Returns:
        A list containing the same integers in reverse order.
    """
    pass
