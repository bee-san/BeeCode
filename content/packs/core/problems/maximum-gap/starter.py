def maximum_gap(nums):
    """Return the largest difference between successive elements of the sorted list.

    Args:
        nums: list of integers, possibly empty, possibly with repeats.

    Returns:
        The largest gap between neighbours in sorted order, or 0 if there are
        fewer than two elements.
    """
    pass
