def move_zeroes(values):
    """Move all zeroes to the end in place, preserving non-zero order.

    Args:
        values: a non-empty list of integers.

    Returns:
        The same list object, rearranged.
    """
    pass
