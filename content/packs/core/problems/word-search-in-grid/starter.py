def word_exists(board, word):
    """Return True if word can be read along an adjacent path in the grid.

    Args:
        board: list of equal-length rows of single characters.
        word: the word to look for.

    Returns:
        True if the word is readable using each cell at most once, else False.
    """
    pass
