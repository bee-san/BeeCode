def longest_consecutive(nums):
    """Return the length of the longest run of consecutive integers in nums.

    Args:
        nums: list of integers, in any order, possibly with duplicates.

    Returns:
        The length of the longest consecutive run, or 0 for an empty list.
    """
    pass
