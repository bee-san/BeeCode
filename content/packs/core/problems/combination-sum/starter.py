def combination_sum(candidates, target):
    """Return every combination of candidates summing to target, with reuse allowed.

    Args:
        candidates: distinct positive integers, in any order.
        target: the sum to reach.

    Returns:
        A list of combinations, each sorted ascending, with no two combinations
        being reorderings of each other. The outer order is not judged.
    """
    pass
