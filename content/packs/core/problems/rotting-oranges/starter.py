def minutes_until_spoiled(grid):
    """Return the minutes until no fresh orange remains, or -1 if one never spoils.

    Args:
        grid: rows of 0 (empty), 1 (fresh) and 2 (spoiled), all the same length.

    Returns:
        The number of minutes, 0 if nothing is fresh, or -1 if some orange is
        unreachable.
    """
    pass
