def rooms_needed(meetings):
    """Return the fewest rooms that can host every meeting.

    A meeting ending exactly when another starts may reuse its room.

    Args:
        meetings: [start, end] pairs in arbitrary order.

    Returns:
        The minimum number of rooms, or 0 if there are no meetings.
    """
    pass
