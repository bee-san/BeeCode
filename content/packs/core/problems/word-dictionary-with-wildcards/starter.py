def run_operations(operations):
    """Replay add and search operations against a wildcard word dictionary.

    Args:
        operations: list of [name, argument] pairs. name is "add" or "search". A
            search argument may contain "." matching any single character.

    Returns:
        A list with one entry per search, in order.
    """
    pass
