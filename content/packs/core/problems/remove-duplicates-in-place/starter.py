def squeeze_duplicates(values):
    """Remove duplicates from a sorted list in place.

    Args:
        values: a non-empty list sorted non-decreasing.

    Returns:
        The number of distinct values, now occupying the first positions.
    """
    pass
