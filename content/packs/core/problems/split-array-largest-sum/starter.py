def split_array(nums, k):
    """Split nums into k contiguous parts, minimising the largest part-sum.

    Args:
        nums: list of non-negative integers, at least one element.
        k: how many non-empty contiguous parts to split into, at most len(nums).

    Returns:
        The smallest achievable value of the largest part-sum.
    """
    pass
