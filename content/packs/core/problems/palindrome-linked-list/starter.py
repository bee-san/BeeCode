def is_palindrome_chain(values):
    """Return whether the chain's values read the same in both directions.

    Args:
        values: the chain's values in order.

    Returns:
        True if the sequence is a palindrome.
    """
    pass
