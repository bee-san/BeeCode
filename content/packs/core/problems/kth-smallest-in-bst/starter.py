def kth_smallest(tree, k):
    """Return the kth smallest value in a binary search tree, counting from 1.

    Args:
        tree: a valid binary search tree as a level-order list, distinct values,
            with None for absent nodes.
        k: which value to return, 1 for the smallest.

    Returns:
        The kth smallest value.
    """
    pass
