def run_operations(operations):
    """Replay hit and count operations over a 300-second window.

    Args:
        operations: a list of [name, timestamp] pairs.

    Returns:
        One integer per count operation, in order.
    """
    pass
