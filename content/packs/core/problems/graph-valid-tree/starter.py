def is_tree(n, edges):
    """Return True if the undirected graph is connected and acyclic.

    Args:
        n: the number of vertices, labelled 0 through n - 1.
        edges: pairs [a, b], with no repeats and no self-loops.

    Returns:
        True if the graph is a tree, else False.
    """
    pass
