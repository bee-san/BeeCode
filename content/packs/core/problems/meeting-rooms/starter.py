def can_use_one_room(meetings):
    """Return whether one room can host every meeting without a clash.

    A meeting ending exactly when another starts is not a clash.

    Args:
        meetings: [start, end] pairs in arbitrary order.

    Returns:
        True if no two meetings overlap, otherwise False.
    """
    pass
