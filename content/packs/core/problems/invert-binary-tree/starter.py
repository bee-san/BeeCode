def invert(tree):
    """Return the mirror image of a binary tree.

    Args:
        tree: the tree as a level-order list, with None for absent nodes.

    Returns:
        The inverted tree as a level-order list, trailing Nones omitted.
    """
    pass
