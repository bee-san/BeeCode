def itinerary(tickets):
    """Return the dictionary-smallest trip from JFK using every ticket exactly once.

    Args:
        tickets: [from, to] pairs of three-letter airport codes, possibly repeated.

    Returns:
        The airports in visiting order, starting with "JFK".
    """
    pass
