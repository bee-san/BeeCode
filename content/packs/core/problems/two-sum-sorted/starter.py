def two_sum_sorted(numbers, target):
    """Return the 0-based indices of the two entries summing to target.

    Args:
        numbers: list of integers sorted in non-decreasing order.
        target: the required sum.

    Returns:
        A list [i, j] with i < j and numbers[i] + numbers[j] == target.
    """
    pass
