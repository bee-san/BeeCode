def product_except_self(nums):
    """Return a list where each position holds the product of all other elements.

    Args:
        nums: list of integers, possibly containing zeros and negatives.

    Returns:
        A list of the same length where result[i] is the product of every element
        of nums other than nums[i]. Do not use division.
    """
    pass
