def bit_counts(limit):
    """Return the set-bit count of every integer from 0 to limit inclusive.

    Args:
        limit: a non-negative integer.

    Returns:
        A list of length limit + 1.
    """
    pass
