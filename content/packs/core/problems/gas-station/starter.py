def starting_station(gas, cost):
    """Return a station index from which the whole circuit can be driven.

    Args:
        gas: the fuel available at each station.
        cost: the fuel needed to drive from each station to the next, wrapping around.

    Returns:
        The starting index, or -1 if no start completes the circuit.
    """
    pass
