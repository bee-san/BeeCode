def merge_all(lists):
    """Merge a list of sorted lists into one sorted list.

    Args:
        lists: list of lists, each sorted ascending. Any may be empty.

    Returns:
        A single sorted list containing every value from every input list.
    """
    pass
