def longest_common_prefix(strs):
    """Return the longest common prefix of every string in strs.

    Args:
        strs: list of strings, possibly empty.

    Returns:
        The longest string that prefixes all of them, or "" if there is none or
        the list is empty.
    """
    pass
