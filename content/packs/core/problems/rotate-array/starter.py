def rotate_right(values, steps):
    """Rotate a list right by steps places, in place.

    Args:
        values: a non-empty list of integers.
        steps: a non-negative number of places to rotate.

    Returns:
        The same list object, rotated.
    """
    pass
