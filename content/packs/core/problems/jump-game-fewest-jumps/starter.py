def fewest_jumps(jumps):
    """Return the fewest jumps needed to reach the last index from index 0.

    Args:
        jumps: the maximum forward move allowed from each index.

    Returns:
        The minimum number of jumps, which is 0 for a single element.
    """
    pass
