def two_sum(nums, target):
    """Return the indices of the two numbers in nums that sum to target.

    Args:
        nums: list of integers.
        target: the integer the two numbers must add to.

    Returns:
        A list of two indices, in any order.
    """
    pass
