def count_decodings(digits):
    """Return how many letter strings could encode to this digit string.

    Letters map to 1 through 26, and a group may not have a leading zero.

    Args:
        digits: a non-empty string of characters 0-9.

    Returns:
        The number of valid decodings, possibly 0.
    """
    pass
