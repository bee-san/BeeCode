def run_operations(operations):
    """Replay add and count operations over a collection of points.

    Args:
        operations: a list of [name, [x, y]] pairs, where name is "add" or "count".

    Returns:
        One integer per count operation, in order.
    """
    pass
