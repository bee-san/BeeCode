def run_operations(operations):
    """Replay stack operations, returning one result per query.

    Args:
        operations: entries of the form [name] or [name, value].

    Returns:
        One result per pop, sum, and size operation, in order.
    """
    pass
