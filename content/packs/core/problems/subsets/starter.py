def subsets(nums):
    """Return every subset of nums.

    Args:
        nums: list of distinct integers, possibly empty.

    Returns:
        A list of lists holding all 2**len(nums) subsets, in any order.
    """
    pass
