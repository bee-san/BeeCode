def min_depth(tree):
    """Return the node count on the shortest root-to-leaf path.

    Args:
        tree: the tree in level order, with None for an absent child.

    Returns:
        The minimum depth, or 0 for an empty tree.
    """
    pass
