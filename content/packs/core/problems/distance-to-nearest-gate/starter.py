def fill_distances(rooms):
    """Fill each empty room with its distance to the nearest gate.

    Args:
        rooms: rows of -1 (wall), 0 (gate) and 2147483647 (empty room).

    Returns:
        The grid with every reachable empty room replaced by its distance. Rooms
        that reach no gate keep 2147483647.
    """
    pass
