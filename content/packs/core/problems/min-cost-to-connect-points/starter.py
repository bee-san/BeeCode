def min_cost_to_connect(points):
    """Return the least total Manhattan cost of connecting every point.

    Args:
        points: distinct [x, y] coordinate pairs.

    Returns:
        The total cost of a minimum spanning tree over the points.
    """
    pass
