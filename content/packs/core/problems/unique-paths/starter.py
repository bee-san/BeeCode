def count_paths(rows, columns):
    """Return the number of right/down paths across a rows by columns grid.

    Args:
        rows: the grid height.
        columns: the grid width.

    Returns:
        The number of distinct paths from the top-left cell to the bottom-right.
    """
    pass
