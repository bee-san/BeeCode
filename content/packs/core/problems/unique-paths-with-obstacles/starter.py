def count_paths_around(grid):
    """Count right-and-down routes from top-left to bottom-right avoiding blocks.

    Args:
        grid: rows of 0 (free) and 1 (blocked).

    Returns:
        The number of routes.
    """
    pass
