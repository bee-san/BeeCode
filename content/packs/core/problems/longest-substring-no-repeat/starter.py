def longest_unique_substring(s):
    """Return the length of the longest substring of s with no repeated character.

    Args:
        s: the string to scan. May be empty.

    Returns:
        The length of the longest contiguous run of distinct characters. 0 for the
        empty string.
    """
    pass
