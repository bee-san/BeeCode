def least_interval(tasks, cooldown):
    """Return the least time to run every task respecting the cooldown.

    Args:
        tasks: task names as single uppercase letters.
        cooldown: minimum units between two runs of the same task.

    Returns:
        The total number of time units, including any necessary idling.
    """
    pass
