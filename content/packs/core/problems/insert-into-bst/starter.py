def insert_into_bst(tree, value):
    """Insert a value into a binary search tree as a new leaf.

    Args:
        tree: the tree as a level-order list, with None for absent nodes and
            trailing Nones omitted. The children of index i are at 2i + 1 and
            2i + 2.
        value: the integer to insert. It is not already in the tree.

    Returns:
        The tree after the insertion, in the same level-order encoding, with
        trailing Nones omitted.
    """
    pass
