def find_words(board, words):
    """Return every word readable along an adjacent path in the grid, sorted.

    Args:
        board: list of equal-length rows of single lowercase characters.
        words: the distinct words to look for.

    Returns:
        The present words, sorted ascending. A single word may not reuse a cell.
    """
    pass
