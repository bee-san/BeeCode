def can_deal(cards, size):
    """Return whether the hand splits into groups of size consecutive cards.

    Args:
        cards: the numbers on the cards.
        size: the required group size.

    Returns:
        True if every card can be placed in such a group, otherwise False.
    """
    pass
