def count_inversions(nums):
    """Count the pairs of positions that are out of order.

    Args:
        nums: list of integers, possibly empty, possibly with repeats.

    Returns:
        The number of pairs i < j with nums[i] > nums[j].
    """
    pass
