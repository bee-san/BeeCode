def lowest_common_ancestor(tree, first, second):
    """Return the value of the lowest common ancestor of two values in a BST.

    Args:
        tree: a valid binary search tree as a level-order list, distinct values,
            with None for absent nodes.
        first: a value present in the tree.
        second: a value present in the tree, in no particular order relative to
            first. It may equal first.

    Returns:
        The value held by the deepest node having both as descendants.
    """
    pass
