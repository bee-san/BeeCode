def last_stone(stones):
    """Return the weight of the single remaining stone, or 0 if none remains.

    Args:
        stones: the stone weights, at least one.

    Returns:
        The final stone's weight, or 0 if all stones were destroyed.
    """
    pass
