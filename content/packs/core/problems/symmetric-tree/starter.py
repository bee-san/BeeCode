def is_symmetric(tree):
    """Return True if the level-order encoded tree mirrors itself about the root.

    Args:
        tree: level-order list of node values, with None for absent nodes.

    Returns:
        True if the left and right subtrees are reflections of one another.
    """
    pass
