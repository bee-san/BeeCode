def count_palindromes(text):
    """Return the number of palindromic contiguous substrings.

    Args:
        text: a non-empty lowercase string.

    Returns:
        The count, treating substrings at different positions as distinct.
    """
    pass
