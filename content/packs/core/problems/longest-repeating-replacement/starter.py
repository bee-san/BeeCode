def longest_run(s, k):
    """Return the longest single-character run obtainable with at most k changes.

    Args:
        s: a non-empty string of uppercase English letters.
        k: the maximum number of characters you may replace.

    Returns:
        The length of the longest achievable run of one repeated character.
    """
    pass
