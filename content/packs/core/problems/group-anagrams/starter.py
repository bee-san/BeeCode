def group_anagrams(strs):
    """Group the words in strs that are anagrams of each other.

    Args:
        strs: list of lowercase strings, possibly empty.

    Returns:
        A list of groups. Each group is a list of words that are anagrams of one
        another, sorted alphabetically, and the list of groups is itself sorted
        alphabetically. Returns [] for empty input.
    """
    pass
