def both_oceans(heights):
    """Return the cells from which water reaches both oceans.

    Water moves to an adjacent cell of height less than or equal to the current one.
    One ocean touches the top and left edges, the other the bottom and right.

    Args:
        heights: rows of cell heights, all the same length.

    Returns:
        A list of [row, column] pairs. The order is not judged.
    """
    pass
