def maximum_xor(nums):
    """Return the largest XOR of any two distinct positions in nums.

    Args:
        nums: list of non-negative integers, possibly empty.

    Returns:
        The maximum value of nums[i] ^ nums[j] for i != j, or 0 if there are
        fewer than two elements.
    """
    pass
