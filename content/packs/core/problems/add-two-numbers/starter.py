def add_digit_lists(left, right):
    """Add two numbers given as least-significant-first digit lists.

    Args:
        left: digits of the first number, least significant first.
        right: digits of the second number, same format.

    Returns:
        The sum in the same format, with no trailing zeros unless it is [0].
    """
    pass
