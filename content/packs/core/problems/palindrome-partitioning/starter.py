def partition(text):
    """Return every way to split text so that every piece is a palindrome.

    Args:
        text: a non-empty lowercase string.

    Returns:
        A list of splittings. Each splitting is a list of pieces in order whose
        concatenation is text. The outer order is not judged.
    """
    pass
