def plus_one(digits):
    """Return the digit list one greater than the given one.

    Args:
        digits: decimal digits, most significant first.

    Returns:
        The digits of the incremented number, most significant first.
    """
    pass
