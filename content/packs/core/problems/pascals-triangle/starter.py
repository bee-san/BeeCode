def triangle(rows):
    """Return the first `rows` rows of Pascal's triangle.

    Args:
        rows: how many rows to build, at least 1.

    Returns:
        A list of rows, each a list of integers.
    """
    pass
