def is_anagram(s, t):
    """Return True if t is an anagram of s.

    Args:
        s: a string of English letters.
        t: a string of English letters.

    Returns:
        True if both strings contain the same characters with the same counts,
        otherwise False. Comparison is case-sensitive.
    """
    pass
