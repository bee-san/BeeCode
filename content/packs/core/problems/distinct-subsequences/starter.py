def count_subsequences(text, pattern):
    """Return how many distinct subsequences of text equal pattern.

    Args:
        text: the string to draw subsequences from.
        pattern: the string to match.

    Returns:
        The number of distinct position sets of text spelling pattern.
    """
    pass
