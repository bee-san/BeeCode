def alphabet_order(words):
    """Deduce the alien alphabet ordering from a sorted word list.

    Args:
        words: words sorted by the unknown ordering.

    Returns:
        The letters appearing in words, in alien order, as a string. Among equally
        valid orderings, the one smallest in ordinary dictionary order. "" if the
        input is inconsistent.
    """
    pass
