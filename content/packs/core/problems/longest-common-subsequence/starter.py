def longest_common(first, second):
    """Return the length of the longest subsequence present in both strings.

    Args:
        first: a lowercase string.
        second: a lowercase string.

    Returns:
        The length of the longest common subsequence, or 0 if there is none.
    """
    pass
