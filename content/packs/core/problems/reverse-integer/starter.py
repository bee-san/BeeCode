def reverse_digits(number):
    """Return the digit-reversal of a signed integer, or 0 if it does not fit in 32 bits.

    Args:
        number: a signed integer within the 32-bit range.

    Returns:
        The reversed value, or 0 on overflow.
    """
    pass
