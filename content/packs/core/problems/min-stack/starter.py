def run_operations(operations):
    """Replay stack operations and return the results of top and min.

    Args:
        operations: list of [name, argument] pairs. name is "push", "pop",
            "top" or "min". The argument is the value for "push" and None
            otherwise.

    Returns:
        A list with one entry per "top" and "min" operation, in order.
    """
    pass
