def sort_colours(nums):
    """Return nums sorted ascending, where every element is 0, 1 or 2.

    Args:
        nums: list whose elements are each 0, 1 or 2.

    Returns:
        The sorted list.
    """
    pass
