def cheapest_descent(rows):
    """Return the least total of a top-to-bottom path.

    Args:
        rows: a triangle, where rows[i] has i + 1 entries.

    Returns:
        The minimum path total.
    """
    pass
