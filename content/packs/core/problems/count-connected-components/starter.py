def count_components(n, edges):
    """Return the number of connected components of an undirected graph.

    Args:
        n: the number of vertices, labelled 0 through n - 1.
        edges: list of [a, b] pairs. May contain duplicates and self-loops.

    Returns:
        The count of connected components. An isolated vertex counts as one.
    """
    pass
