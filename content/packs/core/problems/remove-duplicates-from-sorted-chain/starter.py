def drop_repeats(values):
    """Return the chain's values with adjacent duplicates removed.

    Args:
        values: sorted non-decreasing values, possibly empty.

    Returns:
        The distinct values in ascending order.
    """
    pass
