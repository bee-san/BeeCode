def shortest_path(grid):
    """Return the number of cells on the shortest open path corner to corner.

    Args:
        grid: list of equal-length lists of 0 (open) and 1 (blocked).

    Returns:
        The count of cells on a shortest path from the top-left to the
        bottom-right, inclusive, or -1 if there is none.
    """
    pass
