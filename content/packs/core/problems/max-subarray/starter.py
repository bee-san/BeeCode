def max_subarray_sum(nums):
    """Return the largest sum of any contiguous non-empty subarray of nums.

    Args:
        nums: a non-empty list of integers, which may all be negative.

    Returns:
        The maximum subarray sum. Because the subarray must be non-empty, this is
        never 0 unless a real subarray sums to 0.
    """
    pass
