def single_number(nums):
    """Return the one value in nums that does not appear exactly twice.

    Args:
        nums: list of integers in which every value but one appears twice.

    Returns:
        The value that appears exactly once.
    """
    pass
