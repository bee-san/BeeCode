def combination_sum_no_reuse(candidates, target):
    """Return every combination summing to target using each element at most once.

    Args:
        candidates: positive integers, possibly with repeats, in any order.
        target: the sum to reach.

    Returns:
        A list of combinations, each sorted ascending, with no duplicates among
        them. The outer order is not judged.
    """
    pass
