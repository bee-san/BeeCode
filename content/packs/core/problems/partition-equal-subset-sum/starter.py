def can_partition(values):
    """Return True if the values split into two groups of equal sum.

    Args:
        values: positive integers.

    Returns:
        True if such a split exists, else False.
    """
    pass
