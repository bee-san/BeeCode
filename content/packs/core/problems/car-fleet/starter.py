def count_fleets(target, positions, speeds):
    """Return how many fleets arrive at the target.

    Args:
        target: the destination position.
        positions: distinct starting positions, all less than target.
        speeds: the matching speeds, all positive.

    Returns:
        The number of distinct fleets that arrive.
    """
    pass
