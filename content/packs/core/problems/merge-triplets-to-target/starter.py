def can_reach_target(triplets, target):
    """Return whether position-wise maximum merges can produce target exactly.

    Args:
        triplets: a list of three-element lists.
        target: the three-element list to produce.

    Returns:
        True if target is reachable, otherwise False.
    """
    pass
