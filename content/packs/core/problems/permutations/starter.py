def permutations(nums):
    """Return every ordering of nums.

    Args:
        nums: list of distinct integers, possibly empty.

    Returns:
        A list of lists holding all len(nums)! orderings, in any order.
    """
    pass
