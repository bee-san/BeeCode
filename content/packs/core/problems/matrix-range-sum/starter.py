def rectangle_sums(matrix, queries):
    """Answer rectangle-sum queries against a matrix.

    Args:
        matrix: list of equal-length rows of integers, possibly empty.
        queries: list of [row1, col1, row2, col2] with inclusive corners.

    Returns:
        A list with one sum per query, in the order the queries were given.
    """
    pass
