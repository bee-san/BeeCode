def can_reach_end(jumps):
    """Return whether the last index is reachable from index 0.

    From index i you may move forward by anything from 0 up to jumps[i].

    Args:
        jumps: the maximum forward move allowed from each index.

    Returns:
        True if the last index can be reached, otherwise False.
    """
    pass
