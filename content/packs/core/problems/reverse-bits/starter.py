def reverse_bits(number):
    """Return the 32-bit reversal of an unsigned integer.

    Args:
        number: an integer in [0, 2**32 - 1].

    Returns:
        The value formed by reversing all 32 bits.
    """
    pass
