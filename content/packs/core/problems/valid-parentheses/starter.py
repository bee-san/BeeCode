def is_valid(s):
    """Return True if the brackets in s are balanced.

    Args:
        s: a string containing only the characters ()[]{}.

    Returns:
        True if every bracket is closed by the same type in the correct order,
        otherwise False. The empty string is balanced.
    """
    pass
