def max_coins(values):
    """Return the greatest total earned by bursting every balloon.

    Bursting balloon i earns left * values[i] * right using its neighbours at that moment,
    with a missing neighbour counting as 1.

    Args:
        values: the balloon values.

    Returns:
        The greatest achievable total.
    """
    pass
