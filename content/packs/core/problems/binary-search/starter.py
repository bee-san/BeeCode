def search(nums, target):
    """Return the index of target in the sorted list nums, or -1 if absent.

    Args:
        nums: list of distinct integers sorted in ascending order.
        target: the integer to find.

    Returns:
        The index of target, or -1 if it is not in nums.
    """
    pass
