def is_valid_bst(tree):
    """Return True if the level-order encoded tree is a valid binary search tree.

    Args:
        tree: level-order list of node values, with None for absent nodes.

    Returns:
        True if every node's whole left subtree is smaller and right subtree larger.
    """
    pass
