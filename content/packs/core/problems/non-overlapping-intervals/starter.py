def fewest_removals(intervals):
    """Return the fewest intervals to remove so none of the rest overlap.

    Touching at a point does not count as overlapping.

    Args:
        intervals: [start, end] pairs in arbitrary order.

    Returns:
        The minimum number of removals.
    """
    pass
