def remove_nth_from_end(values, n):
    """Return values with the nth element from the end removed.

    Args:
        values: a non-empty list of integers.
        n: which element to remove, counting from the end. n = 1 is the last.

    Returns:
        The list with that element removed.
    """
    pass
