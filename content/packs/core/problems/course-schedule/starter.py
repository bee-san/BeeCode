def can_finish(n, prerequisites):
    """Return True if every course can be taken subject to its prerequisites.

    Args:
        n: the number of courses, labelled 0 through n - 1.
        prerequisites: list of [course, prerequisite] pairs.

    Returns:
        True if some order satisfies every prerequisite, False if none does.
    """
    pass
