def has_cycle(successors):
    """Return whether the chain from node 0 contains a cycle.

    Args:
        successors: successors[i] is the next index, or -1 for the end.

    Returns:
        True if a node is revisited.
    """
    pass
