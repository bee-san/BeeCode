def longest_palindrome(text):
    """Return the longest palindromic substring, preferring the earliest on a tie.

    Args:
        text: a non-empty lowercase string.

    Returns:
        The longest contiguous palindromic substring.
    """
    pass
