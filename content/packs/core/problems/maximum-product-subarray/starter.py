def max_product(values):
    """Return the largest product of a contiguous non-empty run.

    Args:
        values: integers, possibly negative or zero.

    Returns:
        The largest achievable product.
    """
    pass
