def evaluate(tokens):
    """Evaluate a reverse Polish notation expression.

    Args:
        tokens: list of strings, each an integer literal or one of + - * /.

    Returns:
        The integer value of the expression. Division truncates towards zero.
    """
    pass
