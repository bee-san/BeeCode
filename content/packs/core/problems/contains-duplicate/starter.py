def contains_duplicate(nums):
    """Return True if any value in nums appears more than once.

    Args:
        nums: list of integers, possibly empty.

    Returns:
        True if some value appears at least twice, otherwise False.
    """
    pass
