def overlaps(first, second):
    """Return the intervals covered by both lists.

    Args:
        first: sorted, pairwise-disjoint closed intervals.
        second: likewise.

    Returns:
        The sorted list of intersections, each a [start, end] pair.
    """
    pass
