def shortest_subarray(nums, k):
    """Return the length of the shortest contiguous subarray summing to at least k.

    Args:
        nums: list of integers, which may be negative and may be empty.
        k: the sum the subarray must reach or exceed.

    Returns:
        The length of the shortest such subarray, or -1 if none exists.
    """
    pass
