def run_operations(operations):
    """Replay insert, search and starts_with operations against a prefix tree.

    Args:
        operations: list of [name, argument] pairs. name is "insert", "search" or
            "starts_with".

    Returns:
        A list with one entry per search and starts_with, in order. Inserts
        contribute nothing.
    """
    pass
