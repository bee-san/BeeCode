def course_order(n, prerequisites):
    """Return an order in which all n courses can be taken, or [] if none exists.

    Args:
        n: the number of courses, labelled 0 through n - 1.
        prerequisites: pairs [course, prerequisite].

    Returns:
        A list of all n labels respecting every prerequisite, or an empty list when
        the prerequisites contain a cycle.
    """
    pass
