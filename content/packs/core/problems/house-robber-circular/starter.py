def best_circular_total(values):
    """Return the largest non-adjacent sum where the first and last entries adjoin.

    Args:
        values: non-negative integers, treated as a circle.

    Returns:
        The largest achievable sum.
    """
    pass
