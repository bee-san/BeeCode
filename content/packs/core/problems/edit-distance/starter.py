def edit_distance(source, target):
    """Return the fewest insert/delete/replace edits turning source into target.

    Args:
        source: the starting string, possibly empty.
        target: the string to reach, possibly empty.

    Returns:
        The minimum number of edits.
    """
    pass
