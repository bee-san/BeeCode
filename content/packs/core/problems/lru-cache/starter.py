def run_operations(capacity, operations):
    """Replay get and put operations against an LRU cache.

    Args:
        capacity: the maximum number of entries the cache holds.
        operations: list of operations. A put is ["put", key, value]; a get is
            ["get", key].

    Returns:
        A list with one entry per get, in order. A get for an absent key yields -1.
    """
    pass
