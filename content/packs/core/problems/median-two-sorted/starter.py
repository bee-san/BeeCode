def median_of_sorted(a, b):
    """Return the median of all values in the two ascending lists, as a float.

    Args:
        a: a list of integers sorted ascending. May be empty.
        b: a list of integers sorted ascending. May be empty.
           At least one of the two lists is non-empty.

    Returns:
        The median as a float. With an even total count this is the mean of the two
        middle values, so it may end in .5 even though the inputs are integers.
    """
    pass
