def redundant_edge(edges):
    """Return the edge whose removal leaves a tree, preferring the last such edge.

    Args:
        edges: pairs [a, b] in the order they were added. Exactly one cycle exists.

    Returns:
        The [a, b] pair to remove, in the order it was given.
    """
    pass
