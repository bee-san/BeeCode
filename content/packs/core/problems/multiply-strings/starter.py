def multiply(first, second):
    """Return the product of two decimal strings, as a decimal string.

    Args:
        first: a non-negative integer as a string of digits.
        second: likewise.

    Returns:
        The product as a string, with no leading zeroes.
    """
    pass
