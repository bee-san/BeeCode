def has_path_sum(tree, target):
    """Return whether some root-to-leaf path sums to target.

    Args:
        tree: the tree in level order, with None for an absent child.
        target: the sum to look for.

    Returns:
        True if such a path exists.
    """
    pass
