def insert_interval(intervals, fresh):
    """Insert fresh into a sorted non-overlapping list, merging where they touch.

    Args:
        intervals: [start, end] pairs, sorted by start, none overlapping.
        fresh: the [start, end] pair to insert.

    Returns:
        The sorted non-overlapping list after insertion.
    """
    pass
