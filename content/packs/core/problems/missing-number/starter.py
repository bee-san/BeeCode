def missing_number(values):
    """Return the one value in [0, len(values)] not present in values.

    Args:
        values: distinct integers from 0 to len(values), with one omitted.

    Returns:
        The missing integer.
    """
    pass
