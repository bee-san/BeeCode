def diameter(tree):
    """Return the number of edges on the longest path between any two nodes.

    Args:
        tree: the tree as a level-order list, with None for absent nodes.

    Returns:
        The diameter in edges. 0 for an empty tree or a single node.
    """
    pass
