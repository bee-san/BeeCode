def cheapest_within(n, flights, start, target, k):
    """Return the cheapest price from start to target using at most k stops.

    Args:
        n: the city count, labelled 0 through n - 1.
        flights: [from, to, price] triples describing directed flights.
        start: the departure city.
        target: the destination city.
        k: the maximum number of stops, so at most k + 1 flights.

    Returns:
        The cheapest total price, or -1 when no qualifying route exists.
    """
    pass
