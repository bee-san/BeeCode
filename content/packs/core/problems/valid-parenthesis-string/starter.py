def is_balanced(text):
    """Return whether text can be balanced by choosing meanings for its stars.

    Each star may be "(", ")" or the empty string.

    Args:
        text: a string of "(", ")" and "*".

    Returns:
        True if some choice balances the string, otherwise False.
    """
    pass
