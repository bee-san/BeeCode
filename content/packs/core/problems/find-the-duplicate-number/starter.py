def find_duplicate(nums):
    """Return the one repeated value in nums.

    Args:
        nums: n + 1 integers, each between 1 and n. Exactly one value repeats.

    Returns:
        The repeated value. Do not modify nums.
    """
    pass
