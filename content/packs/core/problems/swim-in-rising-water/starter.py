def least_time(grid):
    """Return the earliest time [0, 0] can reach the opposite corner.

    You may step to an adjacent cell once the water level is at least the elevation
    of both cells. Moving is instantaneous.

    Args:
        grid: an n by n grid of distinct elevations.

    Returns:
        The earliest time the bottom-right cell is reachable.
    """
    pass
