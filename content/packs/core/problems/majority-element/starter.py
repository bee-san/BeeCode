def majority(values):
    """Return the value occurring more than half the time.

    Args:
        values: a non-empty list of integers with a strict majority value.

    Returns:
        The majority value.
    """
    pass
