def delay_time(n, times, start):
    """Return the time until every node receives a signal sent from start.

    Args:
        n: the node count, labelled 1 through n.
        times: [from, to, weight] triples describing directed links.
        start: the node the signal is sent from.

    Returns:
        The time until the last node receives it, or -1 if some node never does.
    """
    pass
