def overlap_area(a, b):
    """Return the area shared by two axis-aligned rectangles.

    Args:
        a: [x1, y1, x2, y2] with (x1, y1) bottom-left and (x2, y2) top-right.
        b: a second rectangle in the same form.

    Returns:
        The overlapping area, or 0 if the rectangles only touch or do not meet.
    """
    pass
