def generate(n):
    """Return every balanced string of n opening and n closing parentheses.

    Args:
        n: the number of pairs, 0 to 8.

    Returns:
        A list of the balanced strings. Order does not matter. For n = 0 the
        answer is [""].
    """
    pass
