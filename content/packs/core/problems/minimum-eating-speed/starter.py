def minimum_speed(piles, hours):
    """Return the smallest integer speed that finishes every pile within hours.

    Args:
        piles: list of positive pile sizes.
        hours: hours available, at least len(piles).

    Returns:
        The minimum workable speed.
    """
    pass
