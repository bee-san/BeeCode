def build_tree(preorder, inorder):
    """Rebuild a binary tree from its pre-order and in-order traversals.

    Args:
        preorder: the pre-order traversal, all values distinct.
        inorder: the in-order traversal of the same tree.

    Returns:
        The tree as a level-order list with None for absent nodes and trailing
        Nones omitted.
    """
    pass
