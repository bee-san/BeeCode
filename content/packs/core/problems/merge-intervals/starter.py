def merge_intervals(intervals):
    """Merge every overlapping interval and return the result sorted by start.

    Args:
        intervals: list of [start, end] pairs with start <= end, in any order.

    Returns:
        A list of non-overlapping [start, end] pairs, sorted by start.
    """
    pass
