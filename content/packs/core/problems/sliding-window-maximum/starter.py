def window_maxima(nums, k):
    """Return the maximum of each contiguous window of length k.

    Args:
        nums: list of integers, non-empty.
        k: window length, at least 1 and at most len(nums).

    Returns:
        A list of len(nums) - k + 1 maxima, left to right.
    """
    pass
