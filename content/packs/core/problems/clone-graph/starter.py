def clone_graph(adjacency):
    """Deep-copy a connected undirected graph and return the copy's adjacency list.

    Args:
        adjacency: adjacency[i] lists the neighbours of node i.

    Returns:
        The adjacency list of the copy, each neighbour list sorted ascending.
    """
    pass
