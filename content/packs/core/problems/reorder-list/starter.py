def reorder(values):
    """Return values reordered as first, last, second, second-to-last, ...

    Args:
        values: list of integers, possibly empty.

    Returns:
        The reordered list.
    """
    pass
