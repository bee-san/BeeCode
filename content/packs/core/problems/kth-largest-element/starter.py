def kth_largest(nums, k):
    """Return the kth largest element of nums, counting duplicates separately.

    Args:
        nums: list of integers, non-empty.
        k: 1-based rank from the largest end; 1 <= k <= len(nums).

    Returns:
        The value at position k when nums is ordered from largest to smallest.
    """
    pass
