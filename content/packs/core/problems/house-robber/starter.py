def best_total(values):
    """Return the largest sum of a subset containing no two adjacent entries.

    Args:
        values: non-negative integers.

    Returns:
        The largest achievable sum, or 0 for an empty list.
    """
    pass
