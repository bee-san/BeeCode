def find_minimum(nums):
    """Return the smallest value in a rotated sorted array of distinct values.

    Args:
        nums: a non-empty list of distinct integers, sorted then rotated left.

    Returns:
        The minimum value.
    """
    pass
