def cheapest_route(nodes, edges, start, target):
    """Return the minimum total cost from start to target, or -1 if unreachable.

    Args:
        nodes: number of nodes, labelled 0 to nodes - 1.
        edges: list of [from, to, cost] with cost >= 0, directed.
        start: node to depart from.
        target: node to reach.

    Returns:
        The cheapest total cost, or -1 if no route exists.
    """
    pass
