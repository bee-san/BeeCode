def range_sum(tree, low, high):
    """Sum the values of a binary search tree that fall within an inclusive range.

    Args:
        tree: the tree as a level-order list, with None for absent nodes. The
            children of index i are at 2i + 1 and 2i + 2.
        low: the smallest value to include.
        high: the largest value to include.

    Returns:
        The sum of every value v with low <= v <= high, or 0 if there are none.
    """
    pass
