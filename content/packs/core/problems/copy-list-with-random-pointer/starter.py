def copy_list(nodes):
    """Deep copy a linked list whose nodes carry an extra random pointer.

    Args:
        nodes: list of [value, random_index] pairs in chain order. random_index is
            an index into nodes, or None.

    Returns:
        A list of [value, random_value] pairs in chain order, describing the copy.
        random_value is the value at the copied node the random pointer aims at, or
        None. Do not modify nodes.
    """
    pass
