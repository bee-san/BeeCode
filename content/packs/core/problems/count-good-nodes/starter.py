def count_good_nodes(tree):
    """Count nodes with no strictly greater value on the path from the root.

    Args:
        tree: the tree as a level-order list, with None for absent nodes.

    Returns:
        The number of good nodes. 0 for an empty tree.
    """
    pass
