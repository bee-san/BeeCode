def inorder(tree):
    """Return the node values of a level-order encoded binary tree, in-order.

    Args:
        tree: level-order list of node values, with None for absent nodes.

    Returns:
        A list of values: left subtree, then node, then right subtree.
    """
    pass
