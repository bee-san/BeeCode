def cheapest_path(grid):
    """Return the least total cost from top-left to bottom-right.

    Args:
        grid: a non-empty rectangular grid of non-negative costs.

    Returns:
        The minimum path cost, counting both endpoints.
    """
    pass
