def least_capacity(weights, days):
    """Return the smallest ship capacity that clears the conveyor in time.

    Args:
        weights: package weights in conveyor order.
        days: the deadline in days.

    Returns:
        The minimum feasible capacity.
    """
    pass
