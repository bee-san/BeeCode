def same_tree(left, right):
    """Return True if two binary trees have identical shape and values.

    Args:
        left: the first tree as a level-order list, with None for absent nodes.
        right: the second tree, in the same form. It may be padded with a
            different number of trailing Nones.

    Returns:
        True if the two trees are identical, otherwise False.
    """
    pass
