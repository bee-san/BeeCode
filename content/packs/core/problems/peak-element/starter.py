def find_peak(nums):
    """Return the index of any element strictly greater than both its neighbours.

    Elements just outside the list count as negative infinity, so the first element
    is a peak if it exceeds the second, and the last is a peak if it exceeds the
    second-to-last.

    Args:
        nums: a non-empty list of integers with no two adjacent values equal.

    Returns:
        The index of any peak. Several may exist; any one of them is correct.
    """
    pass
