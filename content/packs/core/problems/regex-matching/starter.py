def matches(text, pattern):
    """Return whether pattern matches the whole of text.

    The pattern may contain "." for any single character and "*" for zero or more of the
    preceding character.

    Args:
        text: the string to match, possibly empty.
        pattern: the pattern.

    Returns:
        True if the pattern matches the entire text, otherwise False.
    """
    pass
