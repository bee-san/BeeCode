def count_prefixes(words, queries):
    """Return, for each query, how many words begin with it.

    Args:
        words: list of lowercase words.
        queries: list of lowercase prefix strings.

    Returns:
        A list of counts, one per query, in the order the queries were given.
    """
    pass
