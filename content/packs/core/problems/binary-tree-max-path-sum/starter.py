def max_path_sum(tree):
    """Return the largest sum of values along any path in the tree.

    Args:
        tree: a non-empty tree as a level-order list, with None for absent nodes.

    Returns:
        The maximum path sum. May be negative.
    """
    pass
