def max_water(heights):
    """Return the greatest volume of water any two walls can hold.

    Args:
        heights: list of at least two non-negative wall heights.

    Returns:
        The maximum of (j - i) * min(heights[i], heights[j]) over all i < j.
    """
    pass
