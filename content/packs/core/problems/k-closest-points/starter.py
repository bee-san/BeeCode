def k_closest(points, k):
    """Return the k points nearest the origin, sorted by increasing distance.

    Args:
        points: list of [x, y] coordinate pairs. All distances are distinct.
        k: how many to return, at most len(points).

    Returns:
        The k nearest points, ordered from closest to furthest.
    """
    pass
