def letter_combinations(digits):
    """Return every letter string a run of keypad digits could spell.

    Args:
        digits: a string of characters between "2" and "9", possibly empty.

    Returns:
        Every combination taking one letter per digit in order. An empty input
        gives an empty list. The order is not judged.
    """
    pass
