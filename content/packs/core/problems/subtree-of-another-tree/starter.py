def is_subtree(tree, sub):
    """Return True if sub appears in tree as a complete subtree.

    Args:
        tree: the tree to search, as a level-order list with None for absent nodes.
        sub: the tree to look for, in the same form.

    Returns:
        True if some node of tree, with all its descendants, equals sub.
    """
    pass
