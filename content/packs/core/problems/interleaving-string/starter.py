def is_interleaving(first, second, whole):
    """Return whether whole is an interleaving of first and second.

    Args:
        first: one source string.
        second: the other source string.
        whole: the candidate interleaving.

    Returns:
        True if whole interleaves first and second, otherwise False.
    """
    pass
