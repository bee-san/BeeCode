def run_operations(k, initial, additions):
    """Report the kth largest value after each addition.

    Args:
        k: which order statistic to report, 1 for the largest.
        initial: values present before any addition.
        additions: the values to add, in order.

    Returns:
        A list with one entry per addition: the kth largest value at that point.
    """
    pass
