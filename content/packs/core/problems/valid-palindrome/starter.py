def is_palindrome(s):
    """Return True if s is a palindrome ignoring case and non-alphanumerics.

    Args:
        s: a string of printable ASCII characters.

    Returns:
        True if the letters and digits of s read the same in both directions.
    """
    pass
