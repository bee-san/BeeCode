def inorder_successor(tree, target):
    """Return the smallest value in a binary search tree greater than target.

    Args:
        tree: the tree as a level-order list, with None for absent nodes. The
            children of index i are at 2i + 1 and 2i + 2.
        target: the value to find the successor of. It need not be in the tree.

    Returns:
        The smallest value strictly greater than target, or None if there is
        no such value.
    """
    pass
