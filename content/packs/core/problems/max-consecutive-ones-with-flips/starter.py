def longest_run(bits, budget):
    """Return the longest run of ones achievable with at most budget flips.

    Args:
        bits: a list of 0s and 1s.
        budget: how many zeroes may be flipped.

    Returns:
        The length of the longest achievable run.
    """
    pass
